# Dynamic-ML-model
A general ML model that takes any input dataset(numeric), asks you to choose independent and dependent variables, trains it on multiple ml algorithms dynamically, prints predictions, and prints different evaluative scores. It can be used to compare which algorithm gives best accuracy for specific dataset.
